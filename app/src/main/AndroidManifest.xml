<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Roozara"
        android:localeConfig="@xml/locale_config"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.PermissionsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.SleepLockActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.Roozara"
            android:launchMode="singleTask"
            android:taskAffinity=".alarm" />

        <service
            android:name=".service.SleepVpnService"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:exported="true">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="User-scheduled bedtime internet blocking VPN" />
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".receiver.SleepScheduleReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="ir.mehranlatifi83.roozara.ACTION_SLEEP" />
                <action android:name="ir.mehranlatifi83.roozara.ACTION_WAKE" />
                <action android:name="ir.mehranlatifi83.roozara.ACTION_SLEEP_REMINDER" />
            </intent-filter>
        </receiver>

        <!-- mediaPlayback, not specialUse: this service exists to keep an alarm sound
             playing, which is what that type is for. Declaring it correctly also means
             the system treats the playback as media rather than as an unclassified
             background service that may be trimmed. -->
        <service
            android:name=".service.WakeAlarmService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="false" />

        <activity
            android:name=".ui.WaterActivity"
            android:exported="false" />

        <activity
            android:name=".ui.WaterOverlayActivity"
            android:exported="false"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.Roozara"
            android:launchMode="singleTop"
            android:excludeFromRecents="true" />

        <receiver
            android:name=".receiver.WaterReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="ir.mehranlatifi83.roozara.ACTION_WATER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>


        <!-- Keeps the phone unusable during the sleep window. Only an accessibility
             service can see what came to the foreground, which is what closes the
             notification-shade and force-stop routes out of the lock screen. -->
        <service
            android:name=".service.SleepGuardService"
            android:exported="false"
            android:label="@string/guard_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/sleep_guard_service" />
        </service>

        <!-- Exposes only the log directory, so the activity log can be shared with
             whoever is helping diagnose a problem. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
